# ============ Secrets ============
# 真实 API key 文件 —— 永远不要进 git
.env
.env.local
.env*.local
**/.env
**/.env.local

# ============ Node / npm ============
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json

# ============ Rust / Tauri build artifacts ============
target/
**/target/
*.pdb

# ============ Tauri build outputs ============
**/src-tauri/target/
**/src-tauri/gen/

# ============ Vercel ============
.vercel/
**/.vercel/

# ============ Editor ============
.vscode/
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============ Logs / Temporary ============
*.log
logs/
*.tmp
*.bak

# ============ PoC results（含 LLM 调用产物，不必入仓库）============
**/poc/results/

# ============ User-installed binaries / 分发副本 ============
**/downloads/

# ============ Local user data ============
# 决策档案（用户本地数据，应永远在 AppData 不在 repo）
**/decisions.json

# ============ OS / Build temp ============
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
